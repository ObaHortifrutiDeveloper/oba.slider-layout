{
  "definitions": {
    "SliderLayout": {
      "type": "object",
      "properties": {
        "infinite": {
          "default": true,
          "title": "admin/editor.main-layout.infinite",
          "type": "boolean"
        },
        "showNavigationArrows": {
          "default": "always",
          "enum": ["mobileOnly", "desktopOnly", "always", "never"],
          "title": "admin/editor.main-layout.showNavigation",
          "type": "string"
        },
        "showPaginationDots": {
          "default": "always",
          "enum": ["mobileOnly", "desktopOnly", "always", "never"],
          "title": "admin/editor.main-layout.showPaginationDots",
          "type": "string"
        },
        "usePagination": {
          "default": true,
          "title": "admin/editor.main-layout.usePagination",
          "type": "boolean"
        },
        "fullWidth": {
          "default": true,
          "title": "admin/editor.main-layout.sliderFullWidth",
          "description": "admin/editor.main-layout.sliderFullWidthDescription",
          "type": "boolean"
        }
      }
    }
  }
}
